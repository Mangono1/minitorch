#include "minitorch/tensor.h"

namespace minitorch {

// CPU kernels will be expanded here in later versions.
// V0.1 keeps the initial kernels inside Tensor so we can
// validate the core API before introducing the dispatcher.

} // namespace minitorch
